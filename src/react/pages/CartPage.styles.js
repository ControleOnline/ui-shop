export const inlineStyle_139_14 = {
  flex: 1,
};

export const inlineStyle_141_12 = {
  flex: 1,
};

export const inlineStyle_144_14 = (
  {
    theme: theme,
  },
) => ({
  borderRadius: 16,
  borderWidth: 1,
  borderColor: `${theme.primary}30`,
  backgroundColor: `${theme.primary}10`,
  padding: 14,
  marginBottom: 12,
});

export const inlineStyle_153_16 = (
  {
    theme: theme,
  },
) => ({
  color: theme.primary,
  fontSize: 12,
  fontWeight: '800',
});

export const inlineStyle_157_16 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 5,
  color: theme.text,
  fontSize: 22,
  fontWeight: '800',
});

export const inlineStyle_165_20 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 5,
  color: theme.muted,
  fontSize: 13,
});

export const cartSummaryHeaderRowStyle = {
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'space-between',
  gap: 12,
};

export const cartSummaryTitleWrapStyle = {
  flex: 1,
  minWidth: 0,
};

export const cartSummaryClearButtonStyle = ({theme}) => ({
  minWidth: 48,
  height: 48,
  borderRadius: 14,
  borderWidth: 1,
  borderColor: `${theme.danger}26`,
  backgroundColor: `${theme.danger}12`,
  alignItems: 'center',
  justifyContent: 'center',
});

export const cartSummaryClearBadgeStyle = ({theme}) => ({
  position: 'absolute',
  right: -6,
  top: -6,
  minWidth: 20,
  height: 20,
  borderRadius: 10,
  paddingHorizontal: 5,
  backgroundColor: theme.primary,
  alignItems: 'center',
  justifyContent: 'center',
});

export const cartSummaryClearBadgeTextStyle = () => ({
  color: '#fff',
  fontSize: 11,
  fontWeight: '900',
});

export const inlineStyle_172_16 = (
  {
    theme: theme,
  },
) => ({
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  alignItems: 'center',
  justifyContent: 'center',
  paddingVertical: 42,
  paddingHorizontal: 20,
});

export const inlineStyle_184_18 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 10,
  color: theme.text,
  fontSize: 16,
  fontWeight: '700',
});

export const inlineStyle_193_18 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 6,
  color: theme.muted,
  fontSize: 13,
  textAlign: 'center',
});

export const inlineStyle_203_18 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 18,
  minHeight: 42,
  minWidth: 180,
  borderRadius: 12,
  backgroundColor: theme.primary,
  alignItems: 'center',
  justifyContent: 'center',
  paddingHorizontal: 18,
});

export const inlineStyle_214_20 = {
  color: '#fff',
  fontSize: 14,
  fontWeight: '800',
};

export const inlineStyle_222_18 = (
  {
    theme: theme,
  },
) => ({
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  overflow: 'hidden',
});

export const inlineStyle_237_24 = (
  {
    theme: theme,
  },
) => ({
  padding: 12,
  borderBottomWidth: 1,
  borderBottomColor: theme.cardBorder,
  gap: 10,
});

export const inlineStyle_244_26 = {
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
  gap: 8,
};

export const inlineStyle_252_28 = (
  {
    theme: theme,
  },
) => ({
  flex: 1,
  color: theme.text,
  fontSize: 15,
  fontWeight: '700',
});

export const inlineStyle_261_28 = (
  {
    theme: theme,
  },
) => ({
  color: theme.primary,
  fontSize: 14,
  fontWeight: '800',
});

export const inlineStyle_274_30 = (
  {
    theme: theme,
  },
) => ({
  color: theme.muted,
  fontSize: 12,
  lineHeight: 16,
});

export const inlineStyle_295_26 = (
  {
    isMobile: isMobile,
  },
) => ({
  flexDirection: isMobile ? 'column' : 'row',
  alignItems: isMobile ? 'stretch' : 'center',
  justifyContent: 'space-between',
  gap: 10,
});

export const inlineStyle_301_32 = (
  {
    isMobile: isMobile,
  },
) => ({
  maxWidth: isMobile ? '100%' : 190,
});

export const inlineStyle_312_30 = {
  minHeight: 42,
  borderRadius: 10,
};

export const inlineStyle_321_28 = {
  flexDirection: 'row',
  alignItems: 'center',
  gap: 12,
};

export const inlineStyle_326_34 = (
  {
    theme: theme,
  },
) => ({
  color: theme.muted,
  fontSize: 12,
});

export const inlineStyle_331_30 = (
  {
    theme: theme,
  },
) => ({
  width: 34,
  height: 34,
  borderRadius: 10,
  backgroundColor: `${theme.danger}18`,
  alignItems: 'center',
  justifyContent: 'center',
});

export const inlineStyle_353_18 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 12,
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 14,
  gap: 8,
});

export const inlineStyle_363_20 = {
  flexDirection: 'row',
  justifyContent: 'space-between',
};

export const inlineStyle_367_26 = (
  {
    theme: theme,
  },
) => ({
  color: theme.muted,
});

export const inlineStyle_368_26 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontWeight: '700',
});

export const inlineStyle_373_20 = {
  flexDirection: 'row',
  justifyContent: 'space-between',
};

export const inlineStyle_378_22 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 16,
  fontWeight: '700',
});

export const inlineStyle_386_22 = (
  {
    theme: theme,
  },
) => ({
  color: theme.primary,
  fontSize: 20,
  fontWeight: '900',
});

export const inlineStyle_400_12 = (
  {
    isMobile: isMobile,
    theme: theme,
  },
) => ({
  position: 'absolute',
  left: 12,
  right: 12,
  bottom: 12,
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 10,
  flexDirection: isMobile ? 'column' : 'row',
  gap: 10,
});

export const inlineStyle_415_14 = (
  {
    theme: theme,
  },
) => ({
  flex: 1,
  minHeight: 44,
  borderRadius: 12,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  alignItems: 'center',
  justifyContent: 'center',
});

export const inlineStyle_424_20 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontWeight: '700',
});

export const inlineStyle_432_14 = (
  {
    rows: rows,
    theme: theme,
  },
) => ({
  flex: 1,
  minHeight: 44,
  borderRadius: 12,
  backgroundColor: rows.length === 0 ? theme.cardBorder : `${theme.danger}22`,
  alignItems: 'center',
  justifyContent: 'center',
});

export const inlineStyle_445_18 = (
  {
    rows: rows,
    theme: theme,
  },
) => ({
  color: rows.length === 0 ? theme.muted : theme.danger,
  fontWeight: '800',
});

export const inlineStyle_457_14 = (
  {
    rows: rows,
    theme: theme,
  },
) => ({
  flex: 1,
  minHeight: 44,
  borderRadius: 12,
  backgroundColor: rows.length === 0 ? theme.cardBorder : theme.primary,
  alignItems: 'center',
  justifyContent: 'center',
});

export const inlineStyle_467_16 = (
  {
    rows: rows,
    theme: theme,
  },
) => ({
  color: rows.length === 0 ? theme.muted : '#fff',
  fontWeight: '800',
});

export const inlineStyle_185_12 = {
  padding: 14,
  paddingBottom: 130,
};

export const inlineStyle_310_30 = {
  fontSize: 18,
  fontWeight: '700',
};

