import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardItem: {
    borderRadius: 14,
    borderWidth: 1,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  iconButton: {
    width: 34,
    height: 34,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  typeChip: {
    minHeight: 36,
    borderRadius: 999,
    borderWidth: 1,
    paddingHorizontal: 14,
    alignItems: 'center',
    justifyContent: 'center',
  },
  input: {
    borderRadius: 12,
    borderWidth: 1,
    minHeight: 44,
    paddingHorizontal: 12,
    marginTop: 10,
  },
  smallInput: {
    flex: 1,
  },
});

export default styles;

export const inlineStyle_237_14 = {
  flex: 1,
};

export const inlineStyle_239_12 = {
  flex: 1,
};

export const inlineStyle_242_14 = (
  {
    theme: theme,
  },
) => ({
  borderRadius: 16,
  borderWidth: 1,
  borderColor: `${theme.primary}30`,
  backgroundColor: `${theme.primary}10`,
  padding: 14,
});

export const inlineStyle_250_16 = (
  {
    theme: theme,
  },
) => ({
  color: theme.primary,
  fontSize: 12,
  fontWeight: '800',
});

export const inlineStyle_254_16 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 4,
  color: theme.text,
  fontSize: 22,
  fontWeight: '800',
});

export const inlineStyle_262_20 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 6,
  color: theme.muted,
  fontSize: 13,
});

export const inlineStyle_268_14 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 12,
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 14,
});

export const inlineStyle_277_16 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 16,
  fontWeight: '800',
});

export const inlineStyle_282_22 = {
  paddingVertical: 20,
  alignItems: 'center',
};

export const inlineStyle_286_22 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 10,
  color: theme.muted,
});

export const inlineStyle_301_26 = {
  flex: 1,
};

export const inlineStyle_303_24 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 12,
  fontWeight: '800',
});

export const inlineStyle_311_24 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 2,
  color: theme.text,
  fontSize: 16,
  fontWeight: '800',
});

export const inlineStyle_321_24 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 2,
  color: theme.muted,
  fontSize: 12,
});

export const inlineStyle_350_14 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 12,
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 14,
});

export const inlineStyle_359_16 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 16,
  fontWeight: '800',
});

export const inlineStyle_363_20 = {
  marginTop: 12,
  flexDirection: 'row',
  gap: 8,
};

export const inlineStyle_382_24 = (
  {
    selected: selected,
    theme: theme,
  },
) => ({
  color: selected ? theme.primary : theme.text,
  fontWeight: '700',
  textTransform: 'capitalize',
});

export const inlineStyle_441_20 = {
  flexDirection: 'row',
  gap: 8,
};

export const inlineStyle_498_16 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 12,
  borderRadius: 12,
  padding: 10,
  backgroundColor: `${theme.danger}18`,
});

export const inlineStyle_504_22 = (
  {
    theme: theme,
  },
) => ({
  color: theme.danger,
  fontWeight: '700',
});

export const inlineStyle_512_16 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 10,
  borderRadius: 12,
  padding: 10,
  backgroundColor: `${theme.success}18`,
});

export const inlineStyle_518_22 = (
  {
    theme: theme,
  },
) => ({
  color: theme.success,
  fontWeight: '700',
});

export const inlineStyle_526_12 = (
  {
    theme: theme,
  },
) => ({
  position: 'absolute',
  left: 12,
  right: 12,
  bottom: 12,
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 10,
  flexDirection: 'row',
  gap: 10,
});

export const inlineStyle_541_14 = (
  {
    theme: theme,
  },
) => ({
  flex: 1,
  minHeight: 46,
  borderRadius: 12,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  alignItems: 'center',
  justifyContent: 'center',
});

export const inlineStyle_550_20 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontWeight: '700',
});

export const inlineStyle_558_14 = (
  {
    isSaving: isSaving,
    theme: theme,
  },
) => ({
  flex: 1,
  minHeight: 46,
  borderRadius: 12,
  backgroundColor: theme.primary,
  alignItems: 'center',
  justifyContent: 'center',
  opacity: isSaving ? 0.7 : 1,
});

export const inlineStyle_570_22 = {
  color: '#fff',
  fontWeight: '800',
};

export const inlineStyle_271_12 = {
  padding: 14,
  paddingBottom: 126,
};


