export const inlineStyle_85_12 = {
  flex: 1,
};

export const inlineStyle_95_16 = (
  {
    theme: theme,
  },
) => ({
  margin: 14,
  minHeight: 170,
  borderRadius: 18,
  backgroundColor: `${theme.primary}10`,
  alignItems: 'center',
  justifyContent: 'center',
  borderWidth: 1,
  borderColor: `${theme.primary}30`,
  overflow: 'hidden',
});

export const inlineStyle_109_18 = {
  width: '100%',
  height: 190,
};

export const inlineStyle_115_14 = (
  {
    heroUrl: heroUrl,
  },
) => ({
  marginHorizontal: 14,
  marginTop: heroUrl ? 0 : 14,
  marginBottom: 10,
});

export const inlineStyle_120_20 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 22,
  fontWeight: '800',
});

export const inlineStyle_123_20 = (
  {
    theme: theme,
  },
) => ({
  marginTop: 4,
  color: theme.muted,
  fontSize: 13,
});

export const inlineStyle_130_14 = (
  {
    gap: gap,
  },
) => ({
  paddingHorizontal: 14,
  paddingTop: 12,
  paddingBottom: 92,
  flexDirection: 'row',
  flexWrap: 'wrap',
  gap,
});

export const inlineStyle_139_39 = (
  {
    cardWidth: cardWidth,
  },
) => ({
  width: cardWidth,
});

export const inlineStyle_145_18 = (
  {
    theme: theme,
  },
) => ({
  width: '100%',
  borderRadius: 16,
  borderWidth: 1,
  borderColor: theme.cardBorder,
  backgroundColor: theme.surface,
  padding: 24,
  alignItems: 'center',
});

export const inlineStyle_154_24 = (
  {
    theme: theme,
  },
) => ({
  color: theme.text,
  fontSize: 15,
  fontWeight: '700',
});
